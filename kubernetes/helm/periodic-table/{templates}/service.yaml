apiVersion: v1
kind: Service
metadata:
  name: {{ include "periodic-table.fullname" . }}
  labels:
    {{- include "periodic-table.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "periodic-table.selectorLabels" . | nindent 4 }}